if(! whatis build) {
	fn build {
		if(! cd $wrksrc) {
			echo Couldn''t find wrksrc $builddir/$wrksrc
			echo 'wrksrc should probably be one of'
			ls
			exit no_wrksrc
		}
		mk all
	}
}

if(! whatis install) {
	fn install {
		mk install
	}
}
